version: 0.2

phases:
    install:
        runtime-versions:
            java: corretto8

    build:
        commands:
        - mvn clean package
        
artifacts:
    files:
    - 'Dockerfile'
    - '$CODEBUILD_SRC_DIR/target/curated-banking-spring-1.0-SNAPSHOT.jar'
    
