version: 0.2

phases:
    install:
        runtime-versions:
            java: corretto11

    
    build:
        commands:
        - mvn clean install
artifacts:
    files:
    - 'Dockerfile'
    - target/curated-banking-spring 1.0-SNAPSHOT.jar
