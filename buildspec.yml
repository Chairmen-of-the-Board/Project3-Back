version: 0.2

phases:
    install:
        runtime-versions:
            java: corretto8

    build:
        commands:
        - mvn clean package
        
artifacts:
    files:
    - 'Dockerfile'
    secondary-artifacts:
        artifact1:
        base-directory: $CODEBUILD_SRC_DIR/target
        files:
            - 'curated-banking-spring-1.0-SNAPSHOT.jar'
    
